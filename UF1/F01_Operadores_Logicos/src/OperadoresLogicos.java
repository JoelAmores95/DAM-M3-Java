
public class OperadoresLogicos {

	public static void main(String[] args) {
		// Condiciones más complejas
		
		// && -> AND
		// || -> OR (AltGr + 1)
		// ! -> NOT

		// if ((a < 3) && (c == 5)){...
		
		// Para que una condición && sea cierta LAS DOS PARTES se tienen que cumplir.
		
		// Para que una condición con || sea cierta, basta con AL MENOS UNA.
		
		// Una condición ! se tiene que cumplir LO CONTRARIO a la condición inicial
		// Se le considera "unario", solo tiene una condición
		
		// PRIORIDAD
		
		// ! -> && -> ||
		
		// Si la prioridad nos ayuda, no serían estrictamente necesarios los paréntesis, pero son recomendables
		// solo por una lectura más fácil.
	}

}
